# Schema Init

One-shot schema initializer for Neo4j and PostgreSQL databases used by discogsography services.

## Contents

- `schema_init.py`: Main entry point — runs Neo4j and PostgreSQL schema initialization
- `neo4j_schema.py`: Neo4j constraint and index definitions
- `postgres_schema.py`: PostgreSQL table and index definitions
