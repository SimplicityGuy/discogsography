# Promoted profile assets

These rendered files are promoted copies, not editable branding sources.

- Canonical owner: `groovemap-music/infra`, `brand/`
- Canonical source revision: `a3135e2908cc56644d31ba60068f9a81446ac8f8`
- Promotion target: `groovemap-music/.github`, `profile/assets/`
- Integrity record: [`checksums.sha256`](checksums.sha256)

Change the tokens or SVG templates in `infra`, run its deterministic brand renderer, and
then promote the reviewed outputs here. Do not edit these SVGs independently.

The avatar is included as the reviewed input for a manual organization-owner step. GitHub
does not derive the organization avatar from this repository. No font binaries are
embedded or promoted; the SVG banners use a system-font stack.
